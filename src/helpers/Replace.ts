/* eslint-disable prettier/prettier */
export type Replace<T, R> = Omit<T, keyof R> & R; // Estamos exportando um tipo (type) chamado replace que, basicamente, recebe duas tipagens. A primeira tipagem é o tipo original (as props) e uma tipagem de replace. A tipagem de replace representa quais props da tipagem original queremos substituir. Podemos passar, por exemplo, "Replace<NotificationProps, { createAt?: Date}>". --> Nesse caso estamos passando como tipagem original nossas "NotificationProps" e a tipagem de replace estamos passando o "createAt" como sendo opcional e do tipo "Date".
